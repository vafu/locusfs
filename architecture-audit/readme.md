# Architecture Audit

This folder collects read-only architecture review notes. Each file is owned by
one audit pass and should focus on architecture, API cleanliness, reusability,
duplicated structures, redundant files, and boundary issues.

Audit notes should include concrete file references, distinguish observations
from recommendations, and avoid source edits.
