[package]
name = "locusfs-client"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
tokio = { workspace = true, features = ["fs", "io-util", "macros", "net", "rt", "time"] }
tracing = "0.1"
