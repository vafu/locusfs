[package]
name = "locusfs-fuse"
version = "0.1.0"
edition = "2024"

[dependencies]
fuser = "0.17.0"
libc = "0.2.177"
locusfs-core = { path = "../locusfs-core" }
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3.23.0"

